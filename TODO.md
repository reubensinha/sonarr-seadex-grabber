# TODO

## Issues

- [x] Add Search Box on top to search through all Sonarr series.
- [x] None of the Ani-List mappings seem to be working, almost everything seems to be added via title search. (Root cause: per-series Resync never refreshed `tvdb_id` from Sonarr. Fixed - Resync now refreshes Sonarr-sourced fields first.)
- [x] Add mapping via AniList ID is a terrible user experience, if we are manual mapping there should be a text search that then lets the user click the results. (checkboxs -> add, maybe) (Implemented as a live debounced search box with per-result "Add" buttons.)
- [x] There's a choose button, but no download button? (Split into "Prefer" - pick with no network call - and "Download" - submits to qBittorrent.)
- [x] There's a resync button, but no re-search button (Added "Re-search Seadex" - re-queries Seadex for known AniList entries only, doesn't touch AniList mapping.)
- [x] There's no way to remove a mapping, currently there are some series that have automatically mapped to several completely unrelated series. (Added "Remove" - hard-deletes the entry and blacklists its AniList ID so it can't silently reappear via title/TVDB search.)
- [x] I want to be able to see what the last downloaded torrent is on Sonarr for a given series. (Lazy-loaded per series via Sonarr's history API.)
- [x] Display the dates for when eachthe Seadex release was published.
- [x] Have links to Seadex, Sonarr, AniList, and the Torrent, at the relevant locations.
- [x] qBittorrent login fails with "authentication failed" even with correct credentials. (Root cause: qBittorrent 4.1+ rejects `/api/v2/auth/login` as a CSRF measure unless the `Referer`/`Origin` headers match its own host - `qb_authenticate()` sent neither. Fixed in `clients/qbittorrent_client.py`. Note for anyone hitting this: if the container's IP already got auto-banned by qBittorrent's own "ban after consecutive failures" setting from repeated failed attempts, the code fix alone won't help until that ban expires or is cleared in qBittorrent's Web UI options.) **Follow-up**: the hand-rolled Referer/Origin fix alone didn't resolve it in practice, so switched to the `qbittorrent-api` PyPI package instead of maintaining a hand-rolled client - it handles CSRF headers, auth/session mechanics, and API-version differences for us. Installed it and read its actual source rather than going from memory: confirmed `torrents_add()` returns `"Ok."`/`"Fails."` text on older qBittorrent but structured metadata (not a string) on newer versions where a genuine failure raises an exception instead - the success check now handles both. Also now distinguishes a `Forbidden403Error` (banned IP) from a generic `LoginFailed` (bad credentials) in the log output, since those look identical today and are very different problems to fix. Moved the private-torrent/redacted-hash skip check before any connection attempt, so a torrent we're going to skip anyway no longer burns a login attempt - relevant given the ban risk above. **Important**: I can't test this against a real qBittorrent instance myself (no access, and this session's rule is never to trigger real qBittorrent actions) - all testing here is via mocks verifying the integration logic is correct, not that it actually resolves the original failure. If it's still failing after this, check whether it's specifically the ban (the log will now say so explicitly) rather than a library/protocol issue.
- [ ] Unable to find certain Anime via searching on Anilist (Specifically many donghua e.g. Soulmate Adventure Anilist ids: 101843, 141868, 179839)

## New Features

- [ ] When a series becomes available on Seadex, unmonitor that season/episodes, from Sonarr. Or at least have a button to let me do that manually, if we are unable to map the Seadex entry to a particular season. (Deferred - season-level mapping between Sonarr seasons and AniList entries doesn't exist yet.)
- [x] Currently when removing a auto-matched series, it is added to a blacklist that prevents auto-matching in the future. Make this blacklist visible and be able to remove things from the blacklist. (May require creating a seperate settings page, in order to keep the UI clean) (Added a new `/settings` page, linked from the top nav, listing every blacklisted AniList ID with a link out to AniList and an "Un-blacklist" button. Un-blacklisting only clears the block - it doesn't re-add the mapping itself.)
- [x] UI to adjust how often the schedular runs (Same settings page - an hours input that persists to a new `data/settings.json` override, so `config.yaml` never gets rewritten. If the `SYNC_INTERVAL` environment variable is set, the interval is locked and shown read-only instead - matches how an operator would want to pin it in a container deployment. Takes effect from the next scheduled run, doesn't interrupt an in-progress wait.)

## Next Up

- [x] **Show SeaDex release group + notes per torrent.** Confirmed live against AniList ID 99426: each torrent record has a `releaseGroup` field (e.g. `Stye`, `MegaMTBB`, `Dubarctica`), and each collection entry has a `notes` field (one shared note block per AniList ID explaining the release groups). Both now captured (`clients/seadex_client.py`) and shown per torrent/entry (`webapp/templates/dashboard.html`).
- [x] **UI clarity pass.** Series title is now visually heavier than the AniList entry title beneath it (which is smaller/muted, with a left-border accent on the entry block). Torrents are now grouped into SeaDex-style release-group cards (a 2-column grid, one card per release group like `MegaMTBB`/`Stye`/`MTBB`/`Dubarctica`) instead of a flat table, with Best/Alt/Dual Audio shown once per card rather than per tracker row - matches the real SeaDex layout, verified against a reference screenshot of AniList 99426.
- [x] **Mark torrents removed from SeaDex.** A previously-tracked torrent that drops out of a fresh Seadex search is now kept (if it was chosen or preferred) and marked `removed_from_seadex`, with `is_best`/`dual_audio` stripped and a "removed from SeaDex" bdge shown. It's excluded from all automatic candidate selection (and can't be re-preferred), leaving manual Download as the only action - self-heals if it reappears on SeaDex later.

- [x] Add Sort and filter options to page, default to sorting by last updated on SeaDex. (Captured SeaDex's own collection-entry `updated` timestamp - separate from a torrent's publish date. Sort dropdown defaults to most-recently-updated-first, with Title A-Z and Sonarr ID as alternates; two filter checkboxes for ignore-status and needs-a-pick, client-side like the existing search box.)
- [x] Certain entries on Seadex actually point to a search result page on Nyaa with multiple torrents (For example "Arknights: RISE FROM EMBER" seadex-id: 177175, Erai-raws entry). The program currently correctly parses each torrent seperately. However, each torrent appears with it's own prefer and download button. But the group of torrents together should act as a single entry. I.e. Clicking prefer marks that Seadex entry  (not the torrents) as prefered. And clicking download would download all of the torrents. (Turned out to be SeaDex's own `groupedUrl` field - non-empty and identical across every torrent in a multi-part release like Erai-raws' 10 per-episode torrents, empty for standalone/batch releases like RABBIT's single 12-episode torrent. Torrents sharing a `grouped_url` now act as one entry: one Prefer, one Download that submits every part, tracked per-torrent so a new episode arriving later only submits itself. Verified live against the real Arknights entry.)
- [x] Display what the current chosen release is. (Added a "chosen: {release group}" pill in the entry header itself, instead of only a small pill buried in the release-grid.)
- [x] Have some way to manually mark a chosen release. (A release has already been downloaded before this application started tracking things, so mark it myself without redownloading) (Does the preffered system serve the same purpose? Investigate.) (Investigated: no, Prefer never sets `chosen` and never touches qBittorrent - it only pauses auto-selection. Added a new "Already have this" action that sets `chosen=True` directly with zero network calls, works even on a `removed_from_seadex` torrent, and marks every part of a multi-part release together.)
- [ ] Nyaa and AB are not the only indexers. Double check that indexer filter is not hardcoded to those 2.

## Release Infrastructure

- [x] Alpha v0.1.0 release prep: Docker/README cleanup + GitHub Actions CI/CD. Fixed `docker-compose.yml`'s environment block (was hardcoding literal placeholders instead of `${VAR}` substitution, so `.env.docker` silently did nothing); consolidated `config.yaml.example`/`config.yaml.template` into one file; untracked the accidentally-committed `data_BACKUP/`; added an explicit "config.yaml is optional" callout to DOCKER.md (every setting checks its env var first). Also fixed a real bug found while documenting the bare-metal path: `python-dotenv` was a declared dependency but never actually called anywhere, so a `.env` file did nothing for `python main.py` - wired up `load_dotenv()` in `core/config.py` (never overrides a real env var already set, so Docker deployments are unaffected). Restructured ReadMe.md to lead with the Docker quick-start (the intended way to run this) instead of the bare-metal steps - includes a real, copy-pasteable `docker-compose.yml` (not just a pointer to DOCKER.md), so people who never clone the repo (just pull the published GHCR image) have something to start from. Also caught and documented a real gotcha while writing it: `WEBHOOK_HOST` defaults to `localhost` (app-level default, meant for bare-metal), which is unreachable from outside a container - `docker-compose.yml` already hardcodes `0.0.0.0` for you, but it's called out explicitly now since it silently breaks a bare `docker run` otherwise (confirmed by hitting it myself during verification). DOCKER.md's environment variable reference corrected to match. Added a `tests/` smoke-test suite (pytest) covering `main.py`'s scoring, merge/chosen/preferred logic, and grouped-release siblings, plus `ruff` for linting (`pyproject.toml`). New `.github/workflows/ci.yml` (lint + test on every PR into `main`) and `.github/workflows/release.yml` (on merge to `main`: re-runs lint/test, computes the next `vX.Y.Z` tag via `.github/scripts/bump_version.py` - patch by default, minor/major via a `bump:minor`/`bump:major` PR label, `v0.1.0` seed if no tags exist yet - pushes the tag, builds and publishes to `ghcr.io/reubensinha/sonarr-seadex-grabber` as `vX.Y.Z` + `latest`, and creates a GitHub Release).

## Long Term

- [x] Generally make Frontend UI better. (Likely involves moving to React/Seperate Frontend Framework, perhaps something in line with my other projects <https://github.com/reubensinha/ln-manager>) (Discussed first: "looks amateurish" turned out to be a visual-design/interaction problem, not something a framework swap would fix by itself - React with the same spacing/colors would look just as flat, and a rewrite would roughly double the project's technical surface for a solo-maintained tool. Went with a redesign on the existing Jinja2+htmx stack instead, aimed at a "professional Sonarr companion" feel:
  - **Color system** replaced with Sonarr's actual theme values, fetched live from `Sonarr/Sonarr`'s own `frontend/src/Styles/Themes/{dark,light}.js` rather than guessed - signature cyan accent `#35c5f4`, real dark/light backgrounds, a persistent dark-navy topbar in both themes (matching how Sonarr stays visually "itself" regardless of light/dark mode), and real success/danger/warning status colors.
  - **Eliminated full-page reloads for every action** (the single biggest complaint - "doing literally anything pushes me back to the top of the page"). Every entry-level route (Ignore/Download/Prefer/Mark-downloaded/Remove) and series-level route (Resync/Re-search/Pause/Resume/add-mapping) now renders and returns just the affected HTML fragment via htmx (already vendored, no new dependency) instead of a redirect - extracted `partials/entry.html` and `partials/series_card.html` as the single source of truth for both the full page and these fragment responses. Verified live (isolated app, fake seeded data - never touched real Sonarr/qBittorrent/data) that responses are genuinely fragments, not full documents, and that entry-level vs. card-level actions swap the right scope (e.g. Ignore swaps the whole card since toggling the last active entry flips the series' "paused" state too). The global "Sync Now" button deliberately still does a full reload - it can add/remove series entirely, so that's actually correct there.
  - **Fixed real responsive bugs** behind "cards don't autoscale, elements wrap off screen even on large screens": `main` was hard-capped at 960px regardless of viewport; several flex rows were missing `flex-wrap`/`min-width: 0`/`overflow-wrap`, the classic cause of long titles refusing to shrink and overflowing their container.
  - **Settings page** rewritten so locked (env-var-controlled) fields render as actual greyed-out `disabled` inputs with a "locked" badge and a hint naming the env var, instead of a plain text notice - and unlocked fields got real form styling (labeled groups, focus rings, consistent widths) instead of loose inputs.
  - Toolbar (search/sort/filter) restyled into a bordered, grouped bar instead of bare inline controls.
  - Out of scope for this round, by explicit agreement: AniList poster art, a full sidebar-nav layout restructure.)
  - **Follow-up**: checkboxes were still browser-default unstyled (only the label wrapper got styled last round), and filtering needed to grow well beyond the original two checkboxes - "series without a matched Seadex entry", by indexer/tracker, by release features (Dual Audio, Best, etc.), "really just the more options the better." Replaced the two toolbar checkboxes with a "Filters" button opening a dropdown panel (the same `FilterMenu` pattern Sonarr itself uses) containing **tristate** controls (any/must-have/must-not-have, cycling on click - a real UI component since native checkboxes can't be user-cycled through a third state) for: Ignored, Has a chosen release, Has a Seadex/AniList mapping, Best, Dual Audio, Removed from SeaDex, Preferred, Private, plus one tristate per indexer/tracker - discovered dynamically by scanning the page's own data (not a hardcoded Nyaa/AB list), so it stays accurate for whatever trackers actually appear. All client-side, no new backend routes - just new `data-*` attributes on the existing entry/card partials (`data-trackers`, `data-has-best`, etc.), computed with Jinja filters already in use elsewhere. A "Reset" clears everything back to "any," and an active-filter-count badge shows on the Filters button. Verified for real this time, not just by inspection - installed jsdom in the scratchpad and ran the actual served HTML/JS through it end-to-end (mapping/tracker/feature filters, search, reset, the count badge), rather than only checking the logic's structure by eye. Also added real `accent-color` styling to `input[type="checkbox"]` generally, closing the original complaint outright even though the toolbar itself no longer uses any.
- [x] Allow for URLs and conifgs for Sonarr and qBittorrent to be set in the UI and instruct users to set it up if blank. (Allow for env var lock similar to sync interval.) (Extended the `SYNC_INTERVAL_LOCKED` pattern to `SONARR_URL`, `SONARR_API_KEY`, `QB_URL`, `QB_USER`, `QB_PASS`, `QB_CATEGORY` - each independently locked if its own env var is set, otherwise adjustable from new "Sonarr connection"/"qBittorrent connection" cards on the Settings page, backed by the same `data/settings.json`. Required converting `clients/sonarr_client.py`/`clients/qbittorrent_client.py` from static imports to dynamic getters so a change actually takes effect without a restart. Secrets (API key, password) use "leave blank to keep current" semantics and are never echoed back into the page's HTML. Dashboard shows a banner linking to Settings when either isn't fully configured. Out of scope: `SONARR_SERIES_TYPE`/`SONARR_TAGS` stay env/config.yaml-only - filtering knobs, not connectivity. **Caught a real testing mistake mid-round**: my first live-verification attempt used a scratch `DATA_DIR` but ran from the real project directory, so `core/config.py` picked up the real `.env`/`config.yaml` regardless and briefly ran a real sync against your actual Sonarr instance (146 series fetched, some real Seadex/AniList lookups) - no torrent was ever chosen or submitted (qBittorrent auth failed first) and `data/known_series.json` was untouched, but it shouldn't have made those real calls at all. Redid all further verification from a fully isolated copy of the app with no `.env`/`config.yaml` reachable at all.)
- [ ] Add Radarr Support.
- [x] When sync interval set to 0, disable automatic sync, make it manual sync only. (The Settings page previously floored any submitted value up to a 5-minute minimum, silently turning "0" into "300 seconds" - now 0 bypasses that floor and means "disabled." Disabling also suppresses the one-time `STARTUP_SCAN` population pass, not just the recurring loop - otherwise every container restart/update would silently re-run it regardless of "manual only" intent. Re-enabling from Settings takes effect within ~60s without a restart, and runs the deferred population pass then if it was pending. Manual "Sync Now" is unaffected either way. Along the way, found and fixed a real separate bug while live-testing this: `STARTUP_SCAN` was never coerced from its env var string to an actual bool, so `STARTUP_SCAN=false` - set in every docker-compose.yml this project has ever been given, including yours - was silently truthy and never actually did anything.)
- [x] Monitor and unmonitor shows. Basically a feature to allows the use case of "I've already watchded this show and am no longer interested in downloading it, regardless of new seadex releases. However, I want to keep the show in my library in case of future seasons, which I do want downloaded" (Investigated first: the existing per-entry `Ignore` flag already provides exactly this - an ignored entry is skipped from Seadex re-search entirely, stays visible in the library, and a genuinely new season discovered later always starts un-ignored, so it's picked up and downloaded normally regardless of a prior pause. The actual gap was just a bulk convenience: added "Pause show"/"Resume show" buttons per series that ignore/un-ignore every currently-known entry in one click, plus a "paused" pill when all entries are ignored - computed live from the existing data each render, no new stored field. Deliberately worded "Pause"/"Resume" rather than "Monitor/Unmonitor" to avoid confusion with the separate, still-deferred item above about Sonarr's own monitoring, which this never touches.)
- [ ] Look into migrating to sqlite database instead of json.
